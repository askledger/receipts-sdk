// The canonical benchmark runner is bench.mjs (plain ESM JS so it works
// after `npm run build` with no extra TypeScript build configuration).
// This .ts stub exists only as a placeholder if you want to port to TS later.
// See: scripts/bench.mjs
export {};
